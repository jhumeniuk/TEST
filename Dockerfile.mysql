# Use the official MySQL 5.7 image from Docker Hub
FROM mysql:5.7

# Set the root password (replace 'password' with your desired password)
ENV MYSQL_ROOT_PASSWORD=password

# Set the default database (replace 'mydatabase' with your desired database name)
ENV MYSQL_DATABASE=mydatabase

# Copy the SQL script to initialize the database (if needed)
COPY init.sql /docker-entrypoint-initdb.d/

# Expose the default MySQL port
EXPOSE 3306
